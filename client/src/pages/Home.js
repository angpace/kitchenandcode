import Carousel from "../components/Carousel/Carousel";
 

function Home () {
    // const FadeUp = batch(Fade(), Sticky());

    return (
       <div>
            <Carousel/>     
        </div>
        
    )
}

export default Home;